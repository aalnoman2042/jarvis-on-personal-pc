# VONDO voice assistant dependencies
#
# Core (needed for FREE mode — no cost, no API key):
SpeechRecognition>=3.10.0 # microphone -> text (uses Google's free web speech API)
pyaudio>=0.2.14           # microphone access backend for SpeechRecognition
pyttsx3>=2.90             # offline text -> speech (Windows SAPI5, fully free)
psutil>=5.9.0             # CPU / RAM / battery info
pyautogui>=0.9.54         # screenshots + volume/media keys
python-dotenv>=1.0.0      # load settings from .env
#
# Brain backends — install the one(s) you use (default is Gemini):
google-genai>=1.0.0        # VONDO_BRAIN=gemini  (free)
groq>=0.11.0               # VONDO_BRAIN=groq    (free)
anthropic>=0.116.0         # VONDO_BRAIN=claude  (paid; keep for later)
# VONDO_BRAIN=free needs none of the three (rule-based, offline fallback).

