<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Talking to Jarvis. getUserMedia inside the WebView cannot ask for the
         microphone unless the app declares it here first; Capacitor's bridge
         then handles the runtime prompt when the page requests audio capture.
         Without these two the mic button fails with NotAllowedError and no
         dialog ever appears, which reads as the button being broken. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- required="false" so the app still installs on a device without one -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Showing Jarvis something through the rear camera. The file input uses
         capture="environment", which the WebView can only honour if the camera
         is declared here; without it the button silently falls back to the
         gallery, which is a lesser thing, not a broken one. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Reminders that arrive at the minute they were set for.
         Android 12 split exact timing out into its own permission, and the one
         the notifications plugin declares — SCHEDULE_EXACT_ALARM — is NOT
         granted by default on Android 13 and later: it has to be found and
         switched on under Alarms & reminders. Until then the system is free to
         delay an alarm to batch it, which is survivable for "your exam is
         tomorrow" and not for "leave in ten minutes".
         USE_EXACT_ALARM is granted at install and is meant for exactly this
         case — an app whose purpose is alarms and reminders. It is the right
         one to hold, and it means nothing has to be toggled by hand.
         (Play Store distribution restricts this permission to alarm and
         calendar apps. Jarvis is sideloaded, and is one anyway.) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
</manifest>
