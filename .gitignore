# --- Secrets: never push your API keys ---
.env

# --- Local AI: multi-GB models and the Ollama runtime ---
"local llm"/
local llm/
local-llm/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# --- Runtime state ---
jarvis.state
# What you and Jarvis actually said to each other — personal, stays local.
jarvis.history.jsonl
jarvis.history.jsonl.tmp
jarvis.facts.json
jarvis.facts.json.tmp

# --- OS / editor noise ---
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# --- v2: the database Jarvis actually remembers into (yours, stays local) ---
vondo.db
vondo.db-wal
vondo.db-shm

# The PC agent's credential. Written by `python -m agent.pair`.
agent.token

# --- v2: the HUD's build output and its dependencies ---
web/node_modules/
web/dist/
web/.vite/
