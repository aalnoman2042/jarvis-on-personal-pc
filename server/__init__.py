"""VONDO's cloud core — the FastAPI service that deploys to Fly.io.

Imports from `core`, never the other way round. Nothing in here may assume a
screen, a microphone or Windows; that is what the PC agent is for.
"""
